pipeline {
    agent any
    stages {
        stage('Clean & Checkout') {
            steps {
                cleanWs()
                git branch: 'main',
                    credentialsId: 'Tocken-Git',
                    url: 'https://github.com/MUNGRAH-Kshemendra/githubeval.git'
            }
        }
        stage('Build-image-nginx') {
            steps {
                sh 'docker build -t my-nginx -f "$WORKSPACE/Docker File" "$WORKSPACE"'
            }
        }
    }
}
